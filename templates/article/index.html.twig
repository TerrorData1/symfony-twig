{% extends 'base.html.twig' %}

{% block title %}Detail Article{% endblock %}

{% block body %}
<div class="d-flex justify-content-center">
<div class="card mb-3" style="max-width: 800px;">
  <div class="row g-0">
    <div class="col-md-4">
      <img src="{{ vich_uploader_asset(recipe, 'thumbnailFile') }}" class="img-fluid rounded-start" alt="...">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h5 class="card-title">{{ recipe.title }}</h5>
		<p class="card-text text-secondary">{{ recipe.category.name | default('') }}</p>
		<p class="card-text">{{ recipe.content }}</p>
		<h5 class="card-title text-secondary">{{ recipe.duration }} €</h5>
		<a href="#" class="btn btn-warning">Ajouter</a>
    <a href="#" class="btn btn-secondary">Voir le panier</a>
      </div>
    </div>
  </div>
</div>
</div>
{% endblock %}
