{% extends 'base.html.twig' %}

{% block title %}Detail Article{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style> 

{# <div class="example-wrapper"> #}
    {# <h1>Hello {{ controller_name }}! ✅</h1> #}

    {# This friendly message is coming from:
    <ul>
        <li>Your controller at <code>/Users/jacquesmaillot/Desktop/TutoSymfony/src/Controller/ArticleController.php</code></li>
        <li>Your template at <code>/Users/jacquesmaillot/Desktop/TutoSymfony/templates/article/index.html.twig</code></li>
    </ul>
</div> #}

           		<div class="d-flex justify-content-center">
			<div class="row row-cols-1 row-cols-md-3 g-4">

			{# {% for recipe in recipes %} #}
					<div class="col" style="width: 270px">

						<div class="card">
							  <img src="{{ vich_uploader_asset(recipe, 'thumbnailFile') }}" class="card-img-top" alt="...">

							<div class="card-body">
								<h5 class="card-title">{{ recipe.title }}</h5>
								<p class="card-text">{{ recipe.category.name | default('') }}</p>
								<p class="card-text">{{ recipe.content }}</p>
								<h5 class="card-title text-secondary">{{ recipe.duration }} $</h5>

								{# <a href="{{ path('admin.recipe.edit', {id: recipe.id}) }}" class="btn btn-secondary">Voir</a> #}

							</div>
						</div>
					</div>
				{# {% endfor %} #}

				</div>
			</div>
{% endblock %}
